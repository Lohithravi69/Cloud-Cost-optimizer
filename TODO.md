# TODO: Create GitHub Repository and Push Files

- [ ] Check if Git is initialized in the directory; initialize if not
- [ ] Add all files to Git staging
- [ ] Commit the files with an initial commit message
- [ ] Check if GitHub CLI (gh) is installed; install if not
- [ ] Authenticate with GitHub using gh (if prompted)
- [ ] Create a private GitHub repository named "CloudCostOptimizer"
- [ ] Add the GitHub repository as remote origin
- [ ] Push the committed files to the repository
